spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173,https://api-be.minhthangerp.space
  jwt:
    secret: test-secret-key-for-unit-tests-only-min-256-bit-1234567890
    expiration-ms: 3600000
