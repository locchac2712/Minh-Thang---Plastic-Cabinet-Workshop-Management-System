spring.application.name=backend
# ==============================================================
# 1. DATASOURCE CONFIG (SUPABASE)
# ==============================================================
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=user=postgres.czsqswylaccmtlnvqemx
spring.datasource.password=DoAnTotNghiep2026
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# 2. JPA / HIBERNATE CONFIG
# ==============================================================
# 'update': T? ??ng s?a b?ng n?u code thay ??i (Ch? dùng lúc Dev)
# 'validate': Ch? ki?m tra xem code có kh?p DB không (Khuyên dùng vì b?n ?ã t?o DB r?i)
spring.jpa.hibernate.ddl-auto=update

# Hi?n th? câu l?nh SQL trong Console ?? d? debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect chu?n cho Postgres
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==============================================================
# 3. SERVER CONFIG
# ==============================================================
server.port=8080
# Cho phép hi?n th? message l?i chi ti?t (d? debug)
spring.web.error.include-message=always
# ==============================================================
# 4. JWT CONFIGURATION
# ==============================================================
# ?ây là chu?i bí m?t ?? ký Token (Ph?i ?? dài và ng?u nhiên)
app.jwtSecret=SuperSecretKeyForThesisProject2026MustBeVeryLongAndSecureToAvoidErrors
# Th?i gian h?t h?n Token (ms) - Ví d?: 86400000 = 1 ngày
app.jwtExpirationMs=86400000